<?php

$debug = true;
$debug_log_mb = 1; //max megabyte logs, -1 no limite
$api_log_mb = 1; //max megabyte logs, -1 no limite
$error_log_mb = 1; //max megabyte logs, -1 no limite

{% if (ck_global_var==1) %}
    {{global_var|raw}}
{% endif %}
{#
$db4_user = '******';
$db4_psw  = '******';

$TSNAMES = '
  (DESCRIPTION =
    (ADDRESS_LIST =
      (ADDRESS = (PROTOCOL = TCP)(HOST = XXX.XXX.XXX.XX)(PORT = 1521))
    )
    (CONNECT_DATA =
      (SID = GOLDPROD)
      (SERVER = DEDICATED)
    )
  )';
#}
{{oci_user_var|raw}}='{{oci_user|raw}}';
{{oci_password_var|raw}}='{{oci_password|raw}}';
{{oci_cn_var|raw}}='{{oci_cn|raw}}';

