<?php

$debug = true;
$debug_log_mb = 1; //max megabyte logs, -1 no limite
$api_log_mb = 1; //max megabyte logs, -1 no limite
$error_log_mb = 1; //max megabyte logs, -1 no limite

{% if (ck_global_var==1) %}
    {{global_var|raw}}
{% endif %}

//{{cn_name|raw}}
{{pdo_user_var|raw}}='{{pdo_user|raw}}';
{{pdo_password_var|raw}}='{{pdo_password|raw}}';
{{pdo_cn_var|raw}}='{{pdo_cn|raw}}';
{{pdo_options_var|raw}}={{pdo_options|raw}}

